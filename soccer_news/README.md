npm install = install dev dependencies dan dependencies
npm run start-dev = starting development dan server
npm run build = start build webpack

isi gcm sender id di manifest.json dan gcmAPIkey di push.js dengan dengan FCM id yg dibuat di firebase
generate vapid keys dengan web push (web-push generate-vapid-keys --json)
isi bagian auth dengan nilai di console

note:
*jalankan dist di web server chrome
*live demo https://myoukaarata.github.io
